/**
 * An enum containing the different types of mouse events that can occur.
 */
public enum MouseEventType {
    CLICKED,
    PRESSED,
    RELEASED,
    ENTERED,
    EXITED,
    DRAGGED,
    MOVED
}
